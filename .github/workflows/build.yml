name: Build Behaviour Pack

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Build Docker artifacts image
        run: docker build --target artifacts -t cront-pack-artifacts:latest .
